import { useAuth } from "@/_core/hooks/useAuth";
import { BookOpen, ChefHat, CircleHelp, CircleUserRound, Compass, CreditCard, Crown, GraduationCap, Home, Lightbulb, LogOut, Menu, MessageCircle, PanelLeftClose, PanelLeftOpen, ShieldCheck, Sparkles } from "lucide-react";
import type { ReactNode } from "react";
import { useEffect, useState } from "react";
import { Link, useLocation } from "wouter";
import { Brand } from "./Brand";
import { PublicShell } from "./PublicShell";
import { Button } from "./ui/button";
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetTrigger } from "./ui/sheet";

const navigation = [
  { href: "/comunidade", label: "Início", icon: Home },
  { href: "/receitas", label: "Receitas", icon: ChefHat },
  { href: "/academia", label: "Jornadas", icon: Compass },
  { href: "/biblioteca", label: "Guias gratuitos", icon: BookOpen },
  { href: "/forum", label: "Comunidade", icon: MessageCircle },
  { href: "/facilitadores", label: "Facilitadores", icon: Lightbulb },
  { href: "/minha-assinatura", label: "Minha assinatura", icon: CreditCard },
  { href: "/ajuda", label: "Ajuda e segurança", icon: CircleHelp },
  { href: "/perfil", label: "Meu perfil", icon: CircleUserRound },
];
const mobileNavigation = navigation.filter(item => ["/comunidade", "/receitas", "/academia", "/biblioteca", "/perfil"].includes(item.href));

function Initials({ name }: { name?: string | null }) { const initials = (name || "UA").split(" ").map(part => part[0]).join("").slice(0, 2).toUpperCase(); return <span className="grid h-10 w-10 place-items-center rounded-full bg-[var(--sage-pale)] text-xs font-extrabold text-[var(--sage-deep)]">{initials}</span>; }
function SideLink({ href, label, icon: Icon, active, onClick, collapsed = false }: { href: string; label: string; icon: typeof Home; active: boolean; onClick?: () => void; collapsed?: boolean }) { return <Link href={href} onClick={onClick} title={collapsed ? label : undefined} aria-label={label} aria-current={active ? "page" : undefined} className={`nav-link flex items-center gap-3 rounded-2xl px-3 py-3 text-sm font-bold ${collapsed ? "justify-center" : ""} ${active ? "bg-white text-[var(--sage-deep)] shadow-[0_8px_18px_rgba(8,31,77,.08)] ring-1 ring-[var(--line)]" : "text-[var(--ink-soft)] hover:bg-white/75 hover:text-[var(--ink)]"}`}><Icon size={18} strokeWidth={1.8} /><span className={collapsed ? "sr-only" : ""}>{label}</span></Link>; }

export function MemberShell({ children, eyebrow, title, description, allowGuest = false }: { children: ReactNode; eyebrow: string; title: string; description: string; allowGuest?: boolean }) {
  const { user, loading, logout } = useAuth();
  const [location, setLocation] = useLocation();
  const [menuOpen, setMenuOpen] = useState(false);
  const [sidebarCollapsed, setSidebarCollapsed] = useState(() => typeof window !== "undefined" && window.localStorage.getItem("ua-sidebar-collapsed") === "true");
  useEffect(() => { window.localStorage.setItem("ua-sidebar-collapsed", String(sidebarCollapsed)); }, [sidebarCollapsed]);
  if (loading) return <div className="min-h-screen bg-[var(--paper)] p-6"><div className="mx-auto h-8 max-w-6xl animate-pulse rounded-full bg-[var(--sage-pale)]" /></div>;
  if (!user && allowGuest) return <PublicShell><div className="mx-auto w-full max-w-7xl px-5 py-10 sm:px-8 lg:px-10"><div className="mb-10 border-b border-[var(--line)] pb-8"><p className="text-[10px] font-extrabold uppercase tracking-[0.2em] text-[var(--sage)]">{eyebrow}</p><h1 className="display-font mt-3 text-4xl font-semibold tracking-[-0.035em] sm:text-5xl">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-[var(--ink-soft)]">{description}</p></div>{children}</div></PublicShell>;
  if (!user) return <div className="page-texture grid min-h-screen place-items-center p-6"><section className="soft-card w-full max-w-lg rounded-[2rem] p-8 text-center sm:p-12"><div className="mx-auto mb-8 w-fit"><Brand /></div><p className="mb-3 text-xs font-extrabold uppercase tracking-[0.18em] text-[var(--sage)]">Área exclusiva da comunidade</p><h1 className="display-font text-4xl font-semibold leading-none">Entre para continuar sua jornada.</h1><p className="mx-auto mt-5 max-w-md text-sm leading-7 text-[var(--ink-soft)]">A participação é gratuita. Entre com sua conta para acessar os materiais, conversas e seu perfil.</p><Button onClick={() => setLocation("/entrar")} className="pressable mt-8 h-12 rounded-xl bg-[var(--sage-deep)] px-6 font-bold text-white hover:bg-[var(--ink)]">Entrar gratuitamente</Button><Link href="/inicio" className="mt-5 block text-sm font-bold text-[var(--sage-deep)] underline underline-offset-4">Voltar para a página inicial</Link></section></div>;
  const isAdmin = ["admin", "master"].includes(user.role);
  const isMaster = user.role === "master";
  const accountLabel = isMaster ? "Admin Master" : user.role === "admin" ? "Administrador" : "Membro";
  const managementLinks = [{ href: "/admin", label: "Administração", icon: ShieldCheck }, ...(isMaster ? [{ href: "/master", label: "Admin Master", icon: Crown }] : [])];
  return <div className="min-h-screen bg-[var(--paper)] text-[var(--ink)]"><header className="sticky top-0 z-30 border-b border-[var(--line)] bg-[rgba(251,249,245,0.92)] backdrop-blur-xl lg:hidden"><div className="flex h-16 items-center justify-between px-5"><div className="flex items-center gap-2"><Sheet open={menuOpen} onOpenChange={setMenuOpen}><SheetTrigger asChild><button type="button" className="grid h-9 w-9 place-items-center rounded-xl border border-[var(--line)] bg-white text-[var(--sage-deep)]" aria-label="Abrir navegação"><Menu size={18} /></button></SheetTrigger><SheetContent side="left" className="w-[290px] border-r border-[var(--line)] bg-[#f7f4ee] p-5"><SheetHeader className="text-left"><SheetTitle><Brand compact /></SheetTitle></SheetHeader>
<nav className="mt-9 flex flex-col gap-1">{navigation.map(item => <SideLink key={item.href} {...item} active={location === item.href} onClick={() => setMenuOpen(false)} />)}{isAdmin ? <div className="mt-4 border-t border-[var(--line)] pt-4">{managementLinks.map(item => <SideLink key={item.href} {...item} active={location === item.href} onClick={() => setMenuOpen(false)} />)}</div> : null}</nav></SheetContent></Sheet><Brand compact /></div><Link href="/perfil"><Initials name={user.name} /></Link></div></header><div className="mx-auto flex w-full max-w-[1760px]"><aside className={`sticky top-0 hidden h-screen shrink-0 flex-col overflow-hidden border-r border-[var(--line)] bg-[#f7f4ee] p-4 transition-[width] duration-200 lg:flex ${sidebarCollapsed ? "w-[5.5rem]" : "w-80"}`}><div className={`flex items-center ${sidebarCollapsed ? "justify-center" : "justify-between"}`}><div className={sidebarCollapsed ? "max-w-[3.5rem] overflow-hidden" : "min-w-0"}><Brand compact={!sidebarCollapsed} /></div><button type="button" onClick={() => setSidebarCollapsed(current => !current)} className="grid h-10 w-10 shrink-0 place-items-center rounded-xl border border-[var(--line)] bg-white text-[var(--sage-deep)] shadow-sm transition-colors hover:bg-[var(--linen)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--sage)]" aria-label={sidebarCollapsed ? "Expandir menu lateral" : "Recolher menu lateral"} title={sidebarCollapsed ? "Expandir menu lateral" : "Recolher menu lateral"}>{sidebarCollapsed ? <PanelLeftOpen size={18} /> : <PanelLeftClose size={18} />}</button></div><div className="mt-8 min-h-0 flex-1 overflow-y-auto pr-1"><div className="flex flex-col gap-1"><p className={`mb-3 px-3 text-[10px] font-extrabold uppercase tracking-[0.2em] text-[var(--ink-soft)] ${sidebarCollapsed ? "sr-only" : ""}`}>Universo</p>{navigation.slice(0, 6).map(item => <SideLink key={item.href} {...item} active={location === item.href} collapsed={sidebarCollapsed} />)}<div className="my-5 border-t border-[var(--line)]" /><p className={`mb-3 px-3 text-[10px] font-extrabold uppercase tracking-[0.2em] text-[var(--ink-soft)] ${sidebarCollapsed ? "sr-only" : ""}`}>Sua conta</p>{navigation.slice(6).map(item => <SideLink key={item.href} {...item} active={location === item.href} collapsed={sidebarCollapsed} />)}{isAdmin ? <><div className="my-5 border-t border-[var(--line)]" /><p className={`mb-3 px-3 text-[10px] font-extrabold uppercase tracking-[0.2em] text-[var(--ink-soft)] ${sidebarCollapsed ? "sr-only" : ""}`}>Gestão</p>{managementLinks.map(item => <SideLink key={item.href} {...item} active={location === item.href} collapsed={sidebarCollapsed} />)}</> : null}</div></div><div className={`mt-4 rounded-2xl border border-[var(--line)] bg-white p-3 ${sidebarCollapsed ? "flex justify-center" : ""}`}><div className={`flex items-center gap-3 ${sidebarCollapsed ? "justify-center" : ""}`}><Initials name={user.name} />{sidebarCollapsed ? null : <><div className="min-w-0 flex-1"><p className="truncate text-sm font-extrabold">{user.name || "Membro"}</p><p className="truncate text-xs text-[var(--ink-soft)]">{accountLabel}</p></div><button type="button" onClick={() => logout()} className="rounded-lg p-2 text-[var(--ink-soft)] hover:bg-[var(--linen)] hover:text-[var(--ink)]" aria-label="Sair"><LogOut size={16} /></button></>}</div></div></aside>
<main className="page-texture min-w-0 flex-1"><div className="mx-auto w-full max-w-[1500px] px-5 py-7 sm:px-8 lg:px-12 lg:py-9"><div className="mb-8 flex items-center justify-between gap-4 rounded-2xl border border-[var(--line)] bg-white/70 px-4 py-3 shadow-sm backdrop-blur sm:px-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[var(--sage-pale)] text-[var(--sage-deep)]"><Sparkles size={16} /></span><div><p className="text-[10px] font-extrabold uppercase tracking-[0.18em] text-[var(--sage)]">Universo Atípico</p><p className="text-xs font-bold text-[var(--ink-soft)]">Receitas, jornadas e possibilidades</p></div></div><Link href="/perfil" className="hidden items-center gap-2 rounded-xl px-2 py-1.5 text-xs font-extrabold text-[var(--ink-soft)] transition hover:bg-white hover:text-[var(--ink)] sm:flex"><Initials name={user.name} /> <span>{user.name || "Meu perfil"}</span></Link></div><div className="mb-10 border-b border-[var(--line)] pb-8"><p className="text-[10px] font-extrabold uppercase tracking-[0.2em] text-[var(--sage)]">{eyebrow}</p><h1 className="display-font mt-3 text-4xl font-semibold tracking-[-0.035em] sm:text-5xl">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-[var(--ink-soft)]">{description}</p></div>{children}</div></main></div><nav className="fixed inset-x-0 bottom-0 z-30 flex border-t border-[var(--line)] bg-white/95 p-2 backdrop-blur lg:hidden">{mobileNavigation.map(item => { const Icon = item.icon; return <Link key={item.href} href={item.href} className={`nav-link flex flex-1 flex-col items-center gap-1 rounded-xl py-2 text-[10px] font-extrabold ${location === item.href ? "text-[var(--sage-deep)]" : "text-[var(--ink-soft)]"}`}><Icon size={18} /><span>{item.label}</span></Link>; })}</nav></div>;
}
export function SectionHeading({ label, title, action }: { label?: string; title: string; action?: ReactNode }) { return <div className="mb-5 flex items-end justify-between gap-4"><div>{label ? <p className="mb-1 text-[10px] font-extrabold uppercase tracking-[0.18em] text-[var(--sage)]">{label}</p> : null}<h2 className="display-font text-3xl font-semibold tracking-[-0.03em]">{title}</h2></div>{action}</div>; }
export function ContentEmpty({ icon: Icon = BookOpen, title, text }: { icon?: typeof BookOpen; title: string; text: string }) { return <div className="rounded-3xl border border-dashed border-[var(--line)] bg-white/60 px-6 py-10 text-center"><span className="mx-auto grid h-11 w-11 place-items-center rounded-2xl bg-[var(--sage-pale)] text-[var(--sage-deep)]"><Icon size={20} /></span><h3 className="display-font mt-4 text-2xl font-semibold">{title}</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[var(--ink-soft)]">{text}</p></div>; }
